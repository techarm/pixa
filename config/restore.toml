# Image restoration prompt configuration
# Matches nanobanana restore_image handler - index.ts:447-458

[defaults]
format = "png"

[prompt]
# restore sends the user's prompt directly alongside the image
# prefix used for generated filenames
filename_prefix = "restore"
