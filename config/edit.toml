# Image editing prompt configuration
# Matches nanobanana editImage() - imageGenerator.ts:543-659

[defaults]
format = "png"

[prompt]
# edit sends the user's prompt directly alongside the image
# prefix used for generated filenames
filename_prefix = "edit"
