services:
  pixa:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - RUST_LOG=info
    volumes:
      - ./data:/app/data # Optional: for persistent storage
    restart: unless-stopped
